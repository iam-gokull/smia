spring:
  application:
    name: config-server
  profiles:
    active: native, dev, git
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
        git:

server:
  port: 8071

#Enable all Spring Boot Actuator endpoints.
management:
  endpoints:
    web:
      exposure:
        include: "*"